Needs to reposition Gizmo using model scale;
Reset gizmo transforms (rotation and scale);
Persist scene data (model id, position, rotation and scale);
Load scene data;
Improve camera controls (orbital);
    Enable to set center (like a empty player)